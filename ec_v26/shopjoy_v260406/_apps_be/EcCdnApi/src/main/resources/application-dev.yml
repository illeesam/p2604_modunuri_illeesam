# application-dev.yml — 개발 서버(NAS dev 배포) 전용.
# DB 접속은 local 과 동일 기본값(공유 개발DB) 사용. CF_JWT_SECRET 은 dev NAS 의 .env 로 주입 —
# 여기 기본값을 안 둔 건 dev 컨테이너가 무심코 local 더미 시크릿으로 뜨는 걸 막기 위함.
logging:
  level:
    com.shopjoy.eccdnapi: INFO
  file:
    path: /app/logs
