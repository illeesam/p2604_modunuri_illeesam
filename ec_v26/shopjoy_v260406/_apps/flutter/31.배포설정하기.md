# 31. 배포 설정하기 (Flutter) — 따라하기

## 사전 준비

- [ ] [`21.구글설정하기.md`](21.구글설정하기.md), [`22.IOS설정하기.md`](22.IOS설정하기.md) 완료
- [ ] Google Play Console 가입 ($25 1회)
- [ ] Apple Developer Program ($99/년)

---

## 1단계: 버전 정책

`pubspec.yaml` 의 `version`:

```yaml
version: 1.0.0+1
```

| 부분 | 의미 | Android | iOS |
|---|---|---|---|
| `1.0.0` | versionName | versionName | CFBundleShortVersionString |
| `+1` | versionCode | versionCode | CFBundleVersion |

빌드 시 자동 적용. 변경 후:

```bash
flutter pub get
```

---

## 2단계: Android Keystore

### 2-1. 키스토어 생성

```powershell
keytool -genkey -v `
  -keystore shopjoy-fo-release.keystore `
  -alias shopjoy-fo `
  -keyalg RSA -keysize 2048 -validity 10000
```

**분실 시 Play Store 업데이트 영구 불가**. 안전 보관!

### 2-2. android/key.properties 생성 (gitignore)

```properties
storePassword=YOUR_STORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
keyAlias=shopjoy-fo
storeFile=../../shopjoy-fo-release.keystore
```

### 2-3. android/app/build.gradle 수정

```gradle
def keystoreProperties = new Properties()
def keystorePropertiesFile = rootProject.file('key.properties')
if (keystorePropertiesFile.exists()) {
    keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
}

android {
    signingConfigs {
        release {
            keyAlias keystoreProperties['keyAlias']
            keyPassword keystoreProperties['keyPassword']
            storeFile keystoreProperties['storeFile'] ? file(keystoreProperties['storeFile']) : null
            storePassword keystoreProperties['storePassword']
        }
    }
    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled true
            shrinkResources true
        }
    }
}
```

### 2-4. .gitignore

```
android/key.properties
*.keystore
android/app/google-services.json
ios/Runner/GoogleService-Info.plist
```

---

## 3단계: 앱 아이콘

### 3-1. 도구 설치

```yaml
dev_dependencies:
  flutter_launcher_icons: ^0.14.1
```

`pubspec.yaml` 하단:

```yaml
flutter_launcher_icons:
  android: "ic_launcher"
  ios: true
  image_path: "assets/icon.png"
  min_sdk_android: 24
  adaptive_icon_background: "#FFF0F4"
  adaptive_icon_foreground: "assets/icon-foreground.png"
```

### 3-2. 1024×1024 PNG 준비

`assets/icon.png` 에 배치 → 실행:

```bash
flutter pub get
dart run flutter_launcher_icons
```

→ Android `mipmap-*` / iOS `Assets.xcassets` 자동 생성.

---

## 4단계: 스플래시

### 4-1. 도구

```yaml
dev_dependencies:
  flutter_native_splash: ^2.4.1
```

`pubspec.yaml`:

```yaml
flutter_native_splash:
  color: "#FFF0F4"
  image: "assets/splash.png"
  android_12:
    image: "assets/splash.png"
    icon_background_color: "#FFF0F4"
```

### 4-2. 적용

```bash
dart run flutter_native_splash:create
```

---

## 5단계: ATS / Cleartext

### Android

`android/app/src/main/AndroidManifest.xml`:

```xml
<application android:usesCleartextTraffic="false" ...>
```

local 환경 cleartext 허용 → Network Security Config:

`android/app/src/main/res/xml/network_security_config.xml`:
```xml
<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
```

`AndroidManifest.xml`:
```xml
<application android:networkSecurityConfig="@xml/network_security_config" ...>
```

### iOS

`ios/Runner/Info.plist`:

```xml
<key>NSAppTransportSecurity</key>
<dict>
    <key>NSAllowsArbitraryLoads</key>
    <false/>
    <key>NSAllowsLocalNetworking</key>
    <true/>
</dict>
```

---

## 6단계: 권한 사유 (iOS)

`ios/Runner/Info.plist`:

```xml
<key>NSCameraUsageDescription</key>
<string>상품 사진 촬영을 위해 카메라 접근 권한이 필요합니다.</string>
<key>NSPhotoLibraryUsageDescription</key>
<string>갤러리에서 상품 이미지를 선택하기 위해 권한이 필요합니다.</string>
<key>NSUserNotificationsUsageDescription</key>
<string>주문/배송 알림을 받기 위해 권한이 필요합니다.</string>
<key>NSFaceIDUsageDescription</key>
<string>관리자 빠른 로그인을 위해 Face ID를 사용합니다.</string>
```

---

## 7단계: 환경별 분리 (Flavor)

### Android (`android/app/build.gradle`)

```gradle
android {
    flavorDimensions "env"
    productFlavors {
        local { dimension "env"; applicationIdSuffix ".local"; resValue "string", "app_name", "ShopJoy Local" }
        dev   { dimension "env"; applicationIdSuffix ".dev"; resValue "string", "app_name", "ShopJoy Dev" }
        prod  { dimension "env"; resValue "string", "app_name", "ShopJoy" }
    }
}
```

빌드:
```bash
flutter build apk --release --flavor prod --dart-define=APP_ENV=prod
flutter build appbundle --release --flavor prod --dart-define=APP_ENV=prod
```

### iOS Scheme

Xcode → Manage Schemes → Duplicate `Runner`:
- `Runner-Local`, `Runner-Dev`, `Runner-Prod`

각 Scheme 의 Build Configuration 별로 다른 Bundle ID:
- Debug-Local: `com.shopjoy.fo.local`
- Release-Prod: `com.shopjoy.fo`

빌드:
```bash
flutter build ios --release --flavor prod --dart-define=APP_ENV=prod
```

---

## 8단계: ProGuard

`android/app/proguard-rules.pro`:

```proguard
-keepattributes *Annotation*
-keep class io.flutter.** { *; }

# Firebase
-keep class com.google.firebase.** { *; }

# WebView
-keep class * extends android.webkit.WebChromeClient
```

---

## 검증 체크리스트

- [ ] `pubspec.yaml` version 증가
- [ ] Android keystore 생성 + 등록
- [ ] iOS Distribution Certificate 활성
- [ ] 앱 아이콘 + 스플래시 갱신
- [ ] Network Security Config (운영 HTTPS 강제)
- [ ] iOS 권한 사유 모두 작성
- [ ] Flavor 분리 (Android + iOS)

## 다음 단계

→ [`32.배포하기.md`](32.배포하기.md)
