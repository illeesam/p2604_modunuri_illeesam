<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shopjoy.ecadminapi.base.ec.st.mapper.StSettleEtcAdjMapper">

    <!-- 정산 기타 조정 조회 조건 -->
    <sql id="stSettleEtcAdjCond">
        <where>
            <if test="siteId != null and siteId != ''">AND a.site_id = #{siteId}</if>
            <if test="typeCd != null and typeCd != ''">AND a.etc_adj_type_cd = #{typeCd}</if>
            <if test="dateStart != null and dateStart != ''">AND a.reg_date &gt;= #{dateStart}::DATE</if>
            <if test="dateEnd   != null and dateEnd   != ''">AND a.reg_date &lt;  (#{dateEnd}::DATE + INTERVAL '1 day')</if>
        </where>
    </sql>

    <!-- 정산 기타 조정 상세 조회 -->
    <select id="selectById" resultType="com.shopjoy.ecadminapi.base.ec.st.data.dto.StSettleEtcAdjDto">
        SELECT  /* base.ec.st :: StSettleEtcAdj :: selectById */
              a.*
            , ste.site_nm AS site_nm
            , cd_seat.code_label AS etc_adj_type_cd_nm
            , cd_ad.code_label AS etc_adj_dir_cd_nm
        FROM st_settle_etc_adj a
            LEFT JOIN sy_site ste ON ste.site_id = a.site_id
            LEFT JOIN sy_code cd_seat ON cd_seat.code_grp = 'SETTLE_ETC_ADJ_TYPE' AND cd_seat.code_value = a.etc_adj_type_cd
            LEFT JOIN sy_code cd_ad ON cd_ad.code_grp = 'ADJ_DIR' AND cd_ad.code_value = a.etc_adj_dir_cd
        WHERE a.settle_etc_adj_id = #{id}
    </select>

    <!-- 정산 기타 조정 목록 조회 -->
    <select id="selectList" resultType="com.shopjoy.ecadminapi.base.ec.st.data.dto.StSettleEtcAdjDto">
        SELECT  /* base.ec.st :: StSettleEtcAdj :: selectList */
              a.*
            , ste.site_nm AS site_nm
            , cd_seat.code_label AS etc_adj_type_cd_nm
            , cd_ad.code_label AS etc_adj_dir_cd_nm
        FROM st_settle_etc_adj a
            LEFT JOIN sy_site ste ON ste.site_id = a.site_id
            LEFT JOIN sy_code cd_seat ON cd_seat.code_grp = 'SETTLE_ETC_ADJ_TYPE' AND cd_seat.code_value = a.etc_adj_type_cd
            LEFT JOIN sy_code cd_ad ON cd_ad.code_grp = 'ADJ_DIR' AND cd_ad.code_value = a.etc_adj_dir_cd
            <include refid="stSettleEtcAdjCond"/>
        <choose>
            <when test="sort == 'id_asc'">ORDER BY a.settle_etc_adj_id ASC</when>
            <when test="sort == 'id_desc'">ORDER BY a.settle_etc_adj_id DESC</when>
            <when test="sort == 'reg_asc'">ORDER BY a.reg_date ASC</when>
            <when test="sort == 'reg_desc'">ORDER BY a.reg_date DESC</when>
            <otherwise>ORDER BY a.reg_date DESC</otherwise>
        </choose>
        <if test="limit != null and offset != null">LIMIT #{limit} OFFSET #{offset}</if>
    </select>

    <!-- 정산 기타 조정 페이징 목록 조회 -->
    <select id="selectPageList" resultType="com.shopjoy.ecadminapi.base.ec.st.data.dto.StSettleEtcAdjDto">
        SELECT  /* base.ec.st :: StSettleEtcAdj :: selectPageList */
              a.*
            , ste.site_nm AS site_nm
            , cd_seat.code_label AS etc_adj_type_cd_nm
            , cd_ad.code_label AS etc_adj_dir_cd_nm
        FROM st_settle_etc_adj a
            LEFT JOIN sy_site ste ON ste.site_id = a.site_id
            LEFT JOIN sy_code cd_seat ON cd_seat.code_grp = 'SETTLE_ETC_ADJ_TYPE' AND cd_seat.code_value = a.etc_adj_type_cd
            LEFT JOIN sy_code cd_ad ON cd_ad.code_grp = 'ADJ_DIR' AND cd_ad.code_value = a.etc_adj_dir_cd
            <include refid="stSettleEtcAdjCond"/>
        <choose>
            <when test="sort == 'id_asc'">ORDER BY a.settle_etc_adj_id ASC</when>
            <when test="sort == 'id_desc'">ORDER BY a.settle_etc_adj_id DESC</when>
            <when test="sort == 'reg_asc'">ORDER BY a.reg_date ASC</when>
            <when test="sort == 'reg_desc'">ORDER BY a.reg_date DESC</when>
            <otherwise>ORDER BY a.reg_date DESC</otherwise>
        </choose>
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 정산 기타 조정 전체 건수 조회 -->
    <select id="selectPageCount" resultType="long">
        SELECT COUNT(a.*)  /* base.ec.st :: StSettleEtcAdj :: selectPageCount */
        FROM st_settle_etc_adj a
            <include refid="stSettleEtcAdjCond"/>
    </select>

    <!-- 정산 기타 조정 선택 필드 수정 -->
    <update id="updateSelective" parameterType="com.shopjoy.ecadminapi.base.ec.st.data.entity.StSettleEtcAdj">
        UPDATE st_settle_etc_adj  /* base.ec.st :: StSettleEtcAdj :: updateSelective */
        <set>
            <if test="settleId != null">settle_id = #{settleId},</if>
            <if test="siteId != null">site_id = #{siteId},</if>
            <if test="etcAdjTypeCd != null">etc_adj_type_cd = #{etcAdjTypeCd},</if>
            <if test="etcAdjDirCd != null">etc_adj_dir_cd = #{etcAdjDirCd},</if>
            <if test="etcAdjAmt != null">etc_adj_amt = #{etcAdjAmt},</if>
            <if test="etcAdjReason != null">etc_adj_reason = #{etcAdjReason},</if>
            <if test="settleEtcAdjMemo != null">settle_etc_adj_memo = #{settleEtcAdjMemo},</if>
            <if test="updBy != null">upd_by = #{updBy},</if>
            <if test="updDate != null">upd_date = #{updDate},</if>
        </set>
        WHERE settle_etc_adj_id = #{settleEtcAdjId}
    </update>

</mapper>
