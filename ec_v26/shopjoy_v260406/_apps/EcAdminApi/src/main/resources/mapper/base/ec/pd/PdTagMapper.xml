<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shopjoy.ecadminapi.base.ec.pd.mapper.PdTagMapper">

    <!-- 태그 조회 조건 -->
    <sql id="pdTagCond">
        <where>
            <if test="siteId != null and siteId != ''">AND t.site_id = #{siteId}</if>
            <if test="kw != null and kw != ''">
                AND (t.tag_nm ILIKE '%' || #{kw} || '%')
            </if>
            <if test="dateStart != null and dateStart != ''">AND t.reg_date &gt;= #{dateStart}::DATE</if>
            <if test="dateEnd   != null and dateEnd   != ''">AND t.reg_date &lt;  (#{dateEnd}::DATE + INTERVAL '1 day')</if>
        </where>
    </sql>

    <!-- 태그 상세 조회 -->
    <select id="selectById" resultType="com.shopjoy.ecadminapi.base.ec.pd.data.dto.PdTagDto">
        SELECT  /* base.ec.pd :: PdTag :: selectById */
              t.*
            , ste.site_nm AS site_nm
        FROM pd_tag t
            LEFT JOIN sy_site ste ON ste.site_id = t.site_id
        WHERE t.tag_id = #{id}
    </select>

    <!-- 태그 목록 조회 -->
    <select id="selectList" resultType="com.shopjoy.ecadminapi.base.ec.pd.data.dto.PdTagDto">
        SELECT  /* base.ec.pd :: PdTag :: selectList */
              t.*
            , ste.site_nm AS site_nm
        FROM pd_tag t
            LEFT JOIN sy_site ste ON ste.site_id = t.site_id
            <include refid="pdTagCond"/>
        <choose>
            <when test="sort == 'id_asc'">ORDER BY t.tag_id ASC</when>
            <when test="sort == 'id_desc'">ORDER BY t.tag_id DESC</when>
            <when test="sort == 'nm_asc'">ORDER BY t.tag_nm ASC</when>
            <when test="sort == 'nm_desc'">ORDER BY t.tag_nm DESC</when>
            <when test="sort == 'reg_asc'">ORDER BY t.reg_date ASC</when>
            <when test="sort == 'reg_desc'">ORDER BY t.reg_date DESC</when>
            <otherwise>ORDER BY t.reg_date DESC</otherwise>
        </choose>
        <if test="limit != null and offset != null">LIMIT #{limit} OFFSET #{offset}</if>
    </select>

    <!-- 태그 페이징 목록 조회 -->
    <select id="selectPageList" resultType="com.shopjoy.ecadminapi.base.ec.pd.data.dto.PdTagDto">
        SELECT  /* base.ec.pd :: PdTag :: selectPageList */
              t.*
            , ste.site_nm AS site_nm
        FROM pd_tag t
            LEFT JOIN sy_site ste ON ste.site_id = t.site_id
            <include refid="pdTagCond"/>
        <choose>
            <when test="sort == 'id_asc'">ORDER BY t.tag_id ASC</when>
            <when test="sort == 'id_desc'">ORDER BY t.tag_id DESC</when>
            <when test="sort == 'nm_asc'">ORDER BY t.tag_nm ASC</when>
            <when test="sort == 'nm_desc'">ORDER BY t.tag_nm DESC</when>
            <when test="sort == 'reg_asc'">ORDER BY t.reg_date ASC</when>
            <when test="sort == 'reg_desc'">ORDER BY t.reg_date DESC</when>
            <otherwise>ORDER BY t.reg_date DESC</otherwise>
        </choose>
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 태그 전체 건수 조회 -->
    <select id="selectPageCount" resultType="long">
        SELECT COUNT(*)  /* base.ec.pd :: PdTag :: selectPageCount */
        FROM pd_tag t
            <include refid="pdTagCond"/>
    </select>

    <!-- 태그 선택 필드 수정 -->
    <update id="updateSelective" parameterType="com.shopjoy.ecadminapi.base.ec.pd.data.entity.PdTag">
        UPDATE pd_tag  /* base.ec.pd :: PdTag :: updateSelective */
        <set>
            <if test="siteId != null">site_id = #{siteId},</if>
            <if test="tagNm != null">tag_nm = #{tagNm},</if>
            <if test="tagDesc != null">tag_desc = #{tagDesc},</if>
            <if test="useCount != null">use_count = #{useCount},</if>
            <if test="sortOrd != null">sort_ord = #{sortOrd},</if>
            <if test="useYn != null">use_yn = #{useYn},</if>
            <if test="updBy != null">upd_by = #{updBy},</if>
            <if test="updDate != null">upd_date = #{updDate},</if>
        </set>
        WHERE tag_id = #{tagId}
    </update>

</mapper>
