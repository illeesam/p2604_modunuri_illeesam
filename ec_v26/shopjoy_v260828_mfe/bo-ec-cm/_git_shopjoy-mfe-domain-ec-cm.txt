shopjoy-mfe-domain-ec-cm

관리단위: 마이크로 도메인 — 홈 > 대시보드 관리 / 사용자 대시보드 관리
          (shopjoy_v260406/pages/bo/ec/cm/ 일부 복제)
현재 위치: shopjoy_v260828_mfe/bo-ec-cm/  (컨테이너 폴더 바로 밑, 다른 도메인
          폴더들과 완전한 형제. "bo-" 는 정렬용이 아니라 의미론적 접두어(관리자 화면))

⭐ 형상관리 단위(git 레포)와 지연로드 단위 분리 — 다른 도메인들(`bo-ec-mb/
  _git_shopjoy-mfe-domain-ec-mb.txt` 참고)과 일관성을 맞추기 위해 2026-08-29
  (같은 날 후속 재구조화)에 manifest.js/pages/를 레포 루트에서 `dashboard/`
  소그룹 하위 디렉터리로 옮겼다(사용자 지시). 소그룹은 지금 1개("대시보드
  관리"/"사용자 대시보드 관리" 두 메뉴 그룹을 이 manifest.js 하나가 함께 담당)뿐이다.

포함 경로: `dashboard/manifest.js`, `dashboard/pages/bo/cm/dashboard/*`,
          `dev.html`(단독 실행용)
등록 화면: 대시보드 관리(CmDashboardMng), 대시보드 항목관리(CmDashboardItemMng),
          대시보드 데이타관리(CmDashboardDataMng), 대시보드 항목배치
          (CmDashboardLayoutMng), 대시보드 메뉴관리(CmDashboardSysMenuMng)
          — 소그룹 "대시보드 관리" / 사용자 대시보드 관리(CmDashboardMyMng),
          사용자 대시보드 메뉴관리(CmDashboardMenuMng) — 소그룹 "사용자 대시보드
          관리". **대메뉴는 bo-home**(원본 소스는 cm 패키지지만 실제 좌측메뉴
          LEFT_MENUS_TAIL.home 소속이라 bo-ap-home 과 같은 메뉴키를 쓴다 — bo-ap-home
          은 group:null(평평)인 EC 대시보드 예시 2개, 이 폴더는 명명된 그룹 2개로
          같은 메뉴 안에 공존)
공유 라이브러리(ESM import): CmDashboardWidgetUtil.js(대시보드 항목 공용 렌더
          유틸, export default) — CmDashboardMng/CmDashboardItemMng/
          CmDashboardDataMng/CmDashboardLayoutMng/CmDashboardMyMng 5개 화면이
          `import ... from './CmDashboardWidgetUtil.js'` 로 직접 참조(예전
          window.cmDashWidgetUtil 전역 방식에서 전환, bo-zd/simul의 ZdSimulBase.js
          와 동일 패턴). CmDashboardSysMenuMng/CmDashboardMenuMng 은 미의존
원본: shopjoy_v260406(실제 프로덕션)의 pages/bo/ec/cm/ 를 그대로 복사(2026-08-29,
     원본은 전혀 수정하지 않음). 원본 페이지ID가 이미 프로젝트 전체에서 유일해서
     `bo-` 접두어 하나만 붙였다.
셸 의존(런타임 계약): window.MFE_REGISTRY, window.boApiSvc/coApiSvc, window.coUtil/boUtil,
          window.boApp, echarts(CoEchartComp), 셸이 등록한 공용 컴포넌트
