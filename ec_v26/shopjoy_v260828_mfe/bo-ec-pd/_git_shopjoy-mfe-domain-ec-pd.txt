shopjoy-mfe-domain-ec-pd

관리단위: 마이크로 도메인 — 상품관리 대메뉴 전체 (shopjoy_v260406/pages/bo/ec/pd/ 전체
          복제)
현재 위치: shopjoy_v260828_mfe/bo-ec-pd/  (컨테이너 폴더 바로 밑, 다른 도메인 폴더들과
          완전한 형제. "bo-" 는 정렬용이 아니라 의미론적 접두어(관리자 화면))

⭐ 2026-08-29 구조 재설계 — "형상관리 단위(git 레포)"와 "지연로드 단위"를 분리
  (bo-ec-mb/_git_shopjoy-mfe-domain-ec-mb.txt 와 동일한 설계):
  - **형상관리 단위**: 이 `bo-ec-pd/` 폴더 전체가 레포 하나
  - **지연로드 단위**: 여전히 소그룹(중메뉴) 하나 = manifest.js 하나. 5개 하위
    디렉터리 각각이 독립적으로 지연로드된다

포함 경로:
  - `pd/manifest.js`    + `pd/pages/bo/pd/pd/*`    — 소그룹 "상품" (6화면 + 내부 2)
  - `cate/manifest.js`  + `cate/pages/bo/pd/cate/*` — 소그룹 "카테고리" (2화면 + 내부 1)
  - `opt/manifest.js`   + `opt/pages/bo/pd/opt/*`  — 소그룹 "상품옵션관리" (1화면)
    + `opt/bo-pd-opt-code-mng.html`(독립 팝업, 아래 특수 구조 참고)
  - `tmplt/manifest.js` + `tmplt/pages/bo/pd/tmplt/*` — 소그룹 "상품템플릿" (1화면)
  - `info/manifest.js`  + `info/pages/bo/pd/info/*` — 소그룹 "상품정보관리" (4화면)
  - `dev.html`(레포 전체 단독 실행용 — 5개 manifest.js 전부 정적 로드)

등록 화면(14개, 대메뉴 bo-ec-pd): 상품관리(PdProdMng)+단품/옵션/묶음/세트/사은상품등록
          5종 — 소그룹 "상품" / 카테고리관리+카테고리상품관리 — "카테고리" /
          상품옵션관리 — "상품옵션관리" / 배송템플릿관리 — "상품템플릿" /
          상품리뷰관리+상품Q&A관리+재입고알림+태그관리 — "상품정보관리"

⚠ 네이밍 예외: PdSingleProdMng 등 5개는 PdProdMng 을 <pd-prod-mng fixed-prod-type-cd
          ="..."> 태그로 감싸는 얇은 래퍼라, PdProdMng 의 `name:` 은 접두어 없이
          원본 그대로 'PdProdMng' 유지(전역 컴포넌트 태그 매칭 때문). 메뉴 식별자
          (id)는 별개로 'pd-pd-pdProdMng'
⚠ 특수 구조: `opt/` 의 메뉴 화면(PdOptCodeMngPage.js, ES 모듈)이 실제 UI를 직접
          그리지 않고 <iframe src="bo-pd-opt-code-mng.html">로 완전히 독립된 별도
          HTML(원본 그대로 복제, 이 폴더 안 위치)을 불러온다. 그 안의
          PdOptCodeMng.js 는 원본과 동일하게 classic <script>+window 전역 그대로
          (ESM 전환 대상 아닌 유일한 예외 파일). iframe 상대경로는 "표시 중인
          문서" 기준으로 풀리므로 mfe.html 지연로드 경로에서는 정확히 안 맞을 수
          있다는 원본 이전부터의 알려진 한계를 그대로 승계 — opt/dev.html(따로
          만들 경우) 또는 이 레포 dev.html 처럼 같은 디렉터리 기준일 때만 보장됨
내부 컴포넌트: PdProdDtl/PdProdHist(pd/ 안), PdCategoryDtl(cate/ 안, 별도 라우팅
          대상이라 registerComponents 로만 등록)

원본: shopjoy_v260406(실제 프로덕션)의 pages/bo/ec/pd/ 를 그대로 복사(원본은 전혀
     수정하지 않음). 화면 파일의 id/name(`bo-ec-pd-원래이름`)은 이전 단계(폴더별
     분리 시절)에 이미 이 코드 규칙을 따르고 있어서 이번 재구조화로 변경할 필요가
     없었다.
셸 의존(런타임 계약): window.MFE_REGISTRY, window.boApiSvc/coApiSvc, window.coUtil/boUtil,
          셸이 등록한 공용 컴포넌트
